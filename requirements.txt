# GitHub App Service - Production Requirements
# Based on Workstream 1 v3.0 Specification

# Core Framework
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.6
pydantic==2.5.0
pydantic-settings==2.1.0

# GitHub App Authentication
PyJWT==2.8.0              # For GitHub App JWT generation
cryptography==41.0.7      # For RSA key handling and webhook signatures

# HTTP Client
httpx==0.27.0             # Async GitHub API calls

# Database
sqlalchemy==2.0.23        # ORM for PostgreSQL
asyncpg==0.29.0           # Async PostgreSQL driver
alembic==1.13.0           # Database migrations

# Cache
redis==5.0.1              # Async Redis client

# S3 Storage
boto3==1.34.0             # AWS S3 client for resolution storage

# Logging & Monitoring
structlog==23.2.0         # Structured logging
python-json-logger==2.0.7 # JSON log formatting
prometheus-client==0.19.0 # Prometheus metrics (per Workstream 1 spec)

# Utilities
python-dotenv==1.0.0      # Environment variable management

# Testing (Development)
pytest==7.4.3
pytest-asyncio==0.21.1
pytest-cov==4.1.0
pytest-httpx==0.30.0      # Mock httpx requests (compatible with httpx 0.25+)
fakeredis==2.20.1         # Mock Redis for testing (per Workstream 1 spec)